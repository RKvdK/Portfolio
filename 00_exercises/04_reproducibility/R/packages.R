# This script can be used to load all required packages for the simulation study
# In case one of these packages is not installed yet, uncomment one of the following code commands

# install.packages("Hmisc")
# install.packages("mice")
# install.packages("tidyverse")
# install.packages("haven")

library(Hmisc)
library(mice)
library(tidyverse)
library(haven)
